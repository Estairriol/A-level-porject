from manager import Manager

def main():
	manager = Manager() #sets the manager to the manager class
	manager.mainloop() #This is basically a while loop that runs forever and updates functions whenever needed


#Makes sure that this is the file being ran
if __name__ == "__main__":
	main() 